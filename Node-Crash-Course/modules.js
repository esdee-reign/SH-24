// const importing = require('./people');

// const { people, ages } = require('./people');

// console.log(people, ages);

const os = require('os'); // this is built in node itself
console.log(os.platform(), os.homedir()); 